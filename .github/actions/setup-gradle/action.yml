name: Setup Gradle
description: Shared Java and Gradle environment
runs:
  using: composite
  steps:
    - uses: actions/setup-java@v5
      with:
        java-version: "25"
        distribution: microsoft
    - uses: gradle/actions/setup-gradle@v6
    - shell: bash
      run: chmod +x ./gradlew
